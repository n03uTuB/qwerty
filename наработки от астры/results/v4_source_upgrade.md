# v4: усиление источников скора `femur_roi` и `spine_artifacts`

Дата: 2026-09-25. База сравнения — коммит `d256907` (organizer macro-F1 0.439
на историческом `fold_id`-протоколе).

Скрипт: `scripts/exp_roi_source_real.py` (честный замер на боевом контуре
`src.stack` + функции `src.evaluate`). Сырой лог: `results/roi_source_real.txt`.

## Протоколы

* **`fold_id`** — исторический боевой: 5 фолдов обучения CNN (`fold_id.npy`),
  порог только по train-части (`train_violation.npy`), метрика по val, пул по
  фолдам (совпадает с `python -m src.evaluate --stacked`).
* **`seeds`** — `StratifiedGroupKFold(5)` по `study_uid` × 30 сидов; порог только
  по train-части фолда; метрика — среднее по фолдам и сидам (устойчивее к
  одному разбиению).

Масштаб порога — `cnn` (по train-пробам CNN, исторический harness). Все замеры
только по реальным снимкам (252).

## Что найдено

### 1. Источник `femur_roi` (`cnn` → `fused_bag`) — крупнейший единичный прирост

Критерий `femur_roi` — 7 позитивов из 150. При источнике `cnn` и пороге `f1`
рабочая точка вырождается (на seed-averaged протоколе F1 ≈ 0.005). Смена
источника на гибридный с balanced bagging исправляет это.

Seed-averaged, 30 сидов (порог `mapped`, ROI-режим `f1`, масштаб `cnn`):

| источник ROI | macro | BA | qF1 | qAUC | ROI F1 |
|---|---|---|---|---|---|
| `cnn` (база) | 0.352 | 0.663 | 0.649 | 0.717 | 0.005 |
| `fused` (LogReg) | 0.484 | 0.691 | 0.670 | 0.726 | 0.531 |
| `fused_bag` | 0.490 | 0.695 | 0.673 | 0.731 | 0.559 |
| `geo_lda` | 0.491 | 0.691 | 0.676 | 0.725 | 0.561 |
| `geo_bag` | 0.433 | 0.684 | 0.664 | 0.720 | 0.324 |

Парно по 30 сидам против `cnn`: `fused` Δmacro +0.132 (30/30), `fused_bag`
+0.138 (30/30), `geo_lda` +0.139 (30/30); по BA/qF1/qAUC — тоже 30/30, p<0.001.

На историческом `fold_id`: `fused_bag` — единственный вариант, где **все**
метрики растут (macro 0.439→0.446, BA 0.696→0.700, qF1 0.675→0.677,
qAUC 0.714→0.728); `geo_lda` даёт больший macro (+0.095, P=0.97), но слегка
проседает BA (0.693). Поэтому выбран `fused_bag`.

### 2. Источник `spine_artifacts` (`fused` → `geo_bag`)

Чистая геометрия `art_*`/`atlas_*` с balanced bagging даёт лучший F1 «предметов»,
чем гибрид с CNN:

| конфигурация (при `femur_roi=fused_bag`) | macro | BA | qF1 | qAUC | предметы |
|---|---|---|---|---|---|
| `spine_artifacts=fused` | 0.490 | 0.695 | 0.673 | 0.731 | 0.523 |
| `spine_artifacts=geo_bag` | **0.505** | **0.705** | **0.682** | **0.735** | 0.583 |

Парно по 30 сидам: Δmacro +0.015 (29/30), ΔBA +0.009 (30/30), ΔqF1 +0.009
(30/30), ΔqAUC +0.004 (28/30).

### 3. Отвергнуто (ухудшает хотя бы одну метрику)

* `spine_positioning=geo_lda`: macro −0.013, BA −0.024, qAUC −0.041 (0/30) —
  регрессия.
* `spine_axis=geo_bag`: macro без изменений, но метка «ось» −0.004; QC-метрики
  растут. Оставлено на потом (даёт BA 0.718, qF1 0.698, qAUC 0.745, но трогает
  компоненту «ось»).
* Комбинация со `spine_positioning=geo_lda` — qAUC −0.021 (0/30).

## Итог (принятая конфигурация v4)

```python
CRITERION_SOURCES = {
    "spine_positioning": "fused",
    "spine_axis": "fused",
    "spine_artifacts": "geo_bag",   # было fused
    "femur_positioning": "fused",
    "femur_roi": "fused_bag",       # было cnn
}
```

**Seed-averaged (30 сидов), base → v4:**

| метрика | base (`d256907`) | v4 | Δ (парно, 30/30) |
|---|---|---|---|
| organizer macro-F1 | 0.352 | **0.505** | **+0.153** (p<0.001) |
| укладка | 0.477 | 0.477 | 0 |
| ось | 0.403 | 0.403 | 0 |
| предметы | 0.523 | 0.583 | +0.060 |
| ROI | 0.005 | 0.559 | +0.554 |
| quality_class BA | 0.663 | **0.705** | +0.042 |
| quality_class macro-F1 | 0.649 | **0.682** | +0.033 |
| quality_class ROC-AUC | 0.717 | **0.735** | +0.018 |

**Исторический `fold_id` (`python -m src.evaluate --stacked`), base → v4:**

| метрика | base | v4 | Δ |
|---|---|---|---|
| organizer macro-F1 | 0.439 | **0.460** | +0.021 |
| укладка | 0.476 | 0.476 | 0 |
| ось | 0.438 | 0.438 | 0 |
| предметы | 0.556 | **0.611** | +0.056 |
| ROI | 0.286 | **0.316** | +0.030 |
| quality_class BA | 0.696 | **0.710** | +0.014 |
| quality_class macro-F1 | 0.675 | **0.686** | +0.011 |
| quality_class ROC-AUC | 0.714 | **0.732** | +0.018 |
| quality head ROC-AUC | 0.586 | 0.586 | 0 |

Ни одна метрика не ухудшилась ни на одном протоколе.
