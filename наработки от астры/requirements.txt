pydicom>=3.0.1
numpy>=1.24
pillow>=10.0
scipy>=1.10
# декодеры сжатых пикселей: без них pydicom не откроет JPEG / JPEG 2000
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.1
pylibjpeg-openjpeg>=2.3
# формирование DICOM SC / SR
highdicom>=0.23
# сеть DICOM (C-STORE) и REST Orthanc
pynetdicom>=2.1
requests>=2.31
pytest>=8.0
# работа с реальным набором организатора (dxa_real): метрики, таблицы, разметка в xlsx
scikit-learn>=1.3
pandas>=2.0
openpyxl>=3.1
pypdf>=4.0

# --- гибридный сервис src/ (DXA-QC) ---
opencv-python-headless>=4.8
joblib>=1.3
# torch/torchvision ставятся отдельно (см. Dockerfile, ARG TORCH_INDEX_URL),
# чтобы выбрать CPU или CUDA (H200 -> cu124).
